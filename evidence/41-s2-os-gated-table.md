# S2 — OS-gated files, and the run that executed each

_For inclusion in the PR #13392 body (ticket PRDCT-14240, Kimi Code/CLI discovery + runtime)._
_Stamped at branch HEAD `74bded8213` · date 2026-09-20._

Every OS-gated file/branch this diff touches, with the CI run that executed it and the commit that run ran at. Windows-only branches are reported with scrupulous honesty: the cross-platform Windows test box is **retired** (box `10.2.7.152` was in the eu-west-3 dev VPC, destroyed with that region — PRDCT-12163; no us-east-1 replacement — scanner/CLAUDE.md "Cross-platform test machines").

| File / Branch | OS gate | Executed by (run + job + commit) | Notes |
|---|---|---|---|
| `scanner/internal/scanner/agents/embedded_dsl_linux.json` | Linux-only embedded DSL envelope | **EXECUTED** — scanner-detector-e2e run **35241515207**, job **Detection (linux)** (id 105270858167), commit **7cfd87b032**; run green | Real install + golden compare; produced the `goldens-linux` artifact. |
| `scanner/internal/scanner/agents/embedded_dsl_darwin.json` | macOS-only embedded DSL envelope | **EXECUTED** — scanner-detector-e2e run **35241515207**, job **Detection (macos)** (id 105270857930), commit **7cfd87b032**; run green | Real install + golden compare; produced the `goldens-macos` artifact. |
| kimi golden block in `backend_python/scripts/e2e/scanner_golden/payloads.json` (`"kimi-cli"`, line 11603) | Compared on linux + darwin only (windows embedded envelope carries no kimi-cli entry) | **EXECUTED** — scanner-detector-e2e run **35241515207**, jobs **Detection (linux)** + **Detection (macos)**, commit **7cfd87b032**; run green | Golden compare passed on both platforms; no `goldens-windows` artifact exists (windows carries no kimi-cli, by design). |
| `scanner/internal/installer/kimi_hooks_installer.go` — `installScannerBinary` appends `.exe` (`runtime.GOOS == "windows"`, lines 423–424) | Windows-only | **NOT EXECUTED** (Windows test box retired, PRDCT-12163). Compiles on scanner-tests (windows-latest), scanner-pr run **35241510696**, commit **7cfd87b032** (green), but **no test exercises it** | No kimi installer test exists in `scanner/internal/installer/*_test.go` — nothing constructs `KimiHooksInstaller`; `go test ./...` on windows-latest builds the branch but does not run it. Compiled-only on windows-latest; not executed. |
| `scanner/internal/installer/kimi_hooks_installer.go` — `looksLikeOnyxSidecar` probes `<binary>.exe` (`runtime.GOOS == "windows"`, lines 534–535) | Windows-only | **NOT EXECUTED** (Windows test box retired, PRDCT-12163). Compiles on scanner-tests (windows-latest), scanner-pr run **35241510696**, commit **7cfd87b032** (green), but **no test exercises it** | Same as above — no kimi installer test drives `looksLikeOnyxSidecar`. Compiled-only on windows-latest; not executed. |
| `scanner/internal/scanner/guard_aggregate_test.go` — `runtime.GOOS != "windows"` gate (line 253), **windows arm** | Windows arm = kimi-cli NOT added to `expected` (guard support asserted absent on windows) | **EXECUTED** — scanner-tests (windows-latest), scanner-pr run **35241510696** (id `scanner-tests (windows-latest)`), commit **7cfd87b032**; run green | On windows the gate takes the false path and the test asserts kimi-cli is correctly absent from the guard-supporting set — a real executed assertion on windows-latest (not a skip of the whole test). The linux/darwin arm (kimi-cli expected present) runs on scanner-tests (ubuntu/macos-latest) in the same run. |

## Provenance

- **scanner-detector-e2e run 35241515207** — `head_sha` = `7cfd87b032d2897cf2b16d7cd20e2441d927e790`, conclusion **success**; jobs Detection (macos) ✓, Detection (linux) ✓, Detection (windows) ✓. Artifacts: goldens-linux, goldens-macos, detector-parity-linux, detector-parity-macos.
- **scanner-pr run 35241510696** — `head_sha` = `7cfd87b032`, conclusion **success**; jobs scanner-tests (ubuntu-latest) ✓, scanner-tests (macos-latest) ✓, scanner-tests (windows-latest) ✓. The windows go-test leg runs `go test -v ./...` (scanner-pr.yaml lines 110/137), which builds every package including `internal/installer` and runs `internal/scanner`'s tests on windows-latest.
- Windows real-machine execution of the two `kimi_hooks_installer.go` `.exe` branches is **not available**: the cross-platform Windows box is retired (PRDCT-12163) and there is no kimi installer test to exercise the branch on the windows-latest CI runner.
