C4 — issue-recalc keyset batching: real EXPLAIN + lock footprint at >2 devices
================================================================================
Branch:  factory-eval-v32/PRDCT-14240
Query under test (SHIPPED, backend_python/src/common/repository/asset.py:361,
  AssetRepository.get_asset_id_batch_by_app_definition_ids):
    SELECT asset.id
      FROM assets AS asset
     WHERE asset.app_definition_id IN (:ids)
       AND asset.is_deleted = false
       AND asset.id > :after_id            -- keyset cursor
     ORDER BY asset.id
     LIMIT :batch_size                     -- AGENT_ISSUE_RECALC_BATCH_SIZE = 1000
                                           -- (crud_service/api/routes/access_control.py:823)

Driver: the recalc route pages this keyset in a `while True:` loop, starting ONE
IssueDetectionWorkflowInput per page (≤1000 asset_ids), so the whole-family recalc
never loads the family into a single workflow payload. This EXPLAIN proves the
page is a bounded, short, read-only index scan even when the family is large.

Environment: e2e kind cluster kind-slate, ns onyxd, pod postgres-1, tenant schema
onyx_security. Max real device family on the frozen base is 2, so a family of
30,000 devices was SEEDED INSIDE A TRANSACTION (app_definition_id='c4-explain-probe')
purely to size the query above BOTH gates (25k chunk gate, 5k reproject gate),
then ROLLED BACK — final SELECT proves 0 rows remain (zero pollution).

--------------------------------------------------------------------------------
seeded_family_size = 30000   (INSERT 0 30000, then counted; >2 devices, well above 25k/5k)

=== SHIPPED bounded keyset page (LIMIT 1000) — EXPLAIN (ANALYZE, BUFFERS) ===
 Limit  (cost=8.31..8.32 rows=1 width=8) (actual time=7.469..7.559 rows=1000 loops=1)
   Buffers: shared hit=685
   ->  Sort  (cost=8.31..8.32 rows=1 width=8) (actual time=7.468..7.501 rows=1000 loops=1)
         Sort Key: id
         Sort Method: top-N heapsort  Memory: 49kB
         Buffers: shared hit=685
         ->  Index Scan using asset_app_definition_id on assets asset
               (cost=0.28..8.30 rows=1 width=8) (actual time=0.009..6.024 rows=30000 loops=1)
               Index Cond: ((app_definition_id)::text = 'c4-explain-probe'::text)
               Filter: ((NOT is_deleted) AND (id > 0))
               Buffers: shared hit=682
 Planning Time: 0.176 ms
 Execution Time: 7.605 ms

  Reading: uses the dedicated btree `asset_app_definition_id`; top-N heapsort caps
  output at LIMIT 1000; 685 shared buffer hits, 0 reads from disk; 7.6 ms total.
  A bounded, in-memory, read-only page. Each keyset step advances `id > :after_id`
  so successive pages never re-scan prior ids.

=== lock footprint held by this backend during the read (pg_locks, this pid) ===
 locktype |             rel              |       mode       | granted
----------+------------------------------+------------------+---------
 relation | asset_app_definition_id      | AccessShareLock  | t
 relation | asset_external_scope_id      | AccessShareLock  | t
 relation | asset_is_deleted             | AccessShareLock  | t
 relation | asset_status                 | AccessShareLock  | t
 relation | asset_status_id              | AccessShareLock  | t
 relation | asset_surface                | AccessShareLock  | t
 relation | asset_type_app_definition_id | AccessShareLock  | t
 relation | asset_type_group_key         | AccessShareLock  | t
 relation | assets                       | AccessShareLock  | t
 relation | assets                       | RowExclusiveLock | t   <-- from the SEED INSERT, not the read
 relation | assets_effective_group_key   | AccessShareLock  | t
 relation | assets_group_key_lower       | AccessShareLock  | t
 relation | assets_model_ref             | AccessShareLock  | t
 relation | assets_unique_identifier_key | AccessShareLock  | t

  HONEST NOTE: the RowExclusiveLock on `assets` is taken by the seeding INSERT that
  runs earlier in this SAME transaction (needed only to fabricate the 30k family);
  it is NOT taken by the SELECT. The recalc read path (the query above) acquires
  ONLY AccessShareLock on the table and the indexes it touches — no row locks, no
  FOR UPDATE, no Share/Exclusive table lock. A recalc page cannot block writers.

=== CONTRAST: the REMOVED unbounded whole-family read (no LIMIT) ===
 Index Scan using asset_app_definition_id on assets asset
   (cost=0.28..8.30 rows=1 width=8) (actual time=0.010..5.172 rows=30000 loops=1)
   Index Cond: ((app_definition_id)::text = 'c4-explain-probe'::text)
   Filter: (NOT is_deleted)
 Execution Time: 6.113 ms

  Same index, but with NO LIMIT it emits all 30,000 ids in one shot. The prior code
  fed exactly this whole-family result into a single workflow input. The shipped
  keyset+LIMIT replaces it with 30 bounded 1000-id pages -> 30 bounded workflow
  starts, each carrying ≤1000 ints (see C6 payload evidence for the size math).

=== after ROLLBACK: proof of zero pollution ===
 rows_remaining_after_rollback = 0

Captured: 2026-09-22, kubectl exec -n onyxd postgres-1 -- psql (DBA read path for a
lock/EXPLAIN inspection; not a feature front-door test). Raw session: reproducible
via the SQL in this run.
